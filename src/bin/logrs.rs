#[macro_use]
extern crate log;
// extern crate env_logger;

use std::fs;
use std::path::Path;
use std::{thread, time};

fn main(){
    // let file = fs::metadata("config/log4rs.yml");

    // println!("{:?}", file);

    //log4rs usage
    // let _log = log4rs::init_file("config/log4rs.yaml", Default::default()).unwrap();


    // info!("booting up");
    // error!("error error");
    // trace!("trace up");
    // warn!("warn up");

    // let ten_millis = time::Duration::from_millis(1000);
    // let now = time::Instant::now();

    // thread::sleep(ten_millis);

    // assert!(now.elapsed() >= ten_millis);
    // let _ = env_logger::init().unwrap();
    info!("fuck you");
}

